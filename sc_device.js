function SteamControllerChannel(scDevice, channelId) {
	this.device = scDevice;
	this.channelId = channelId;
	
	function play(channelRoutine) {
		
	}
	
	function send(feedbackData) {
		
	}
}

function SteamControllerDevice(usbDevice) {
	function send() {
		
	}
}

// Channel routines could also work on 2 channels, like panning, in theory.
